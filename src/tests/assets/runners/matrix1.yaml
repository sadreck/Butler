name: Matrix Test
on:
  push:

jobs:
  job1:
    name: This is job 1
    runs-on: ${{ matrix.os }}-${{ matrix.version }}
    strategy:
      matrix:
        os: ['ubuntu', 'windows']
        version: ['18.04', '20.04', '22.04', 2016, 2019, 2022]
    steps:
      - name: Just one step
        shell: bash
        run: ls -ahl /
